export interface Image {
  id?: string,
  authorId?: string,
  author?: string
  date?: any,
  image?: string,
  genres?: string[],
  likedBy?: string[],
  likes?: number,
  rrating?: string,
  score?: number,
  status?: string,
  title?: string,
  views?: number
}
